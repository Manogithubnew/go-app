appName: goapp

namespace: sample-goapp

image:
  repository: 192.168.1.36:5000/goapp
  tag: latest
  pullPolicy: IfNotPresent

containerPort: 8080

service:
  type: NodePort
  port: 8080

env:
  - name: ENV
    value: "staging"

imagePullSecrets:
  - name: regcred
